<Window x:Class="foxTasker.QueueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:foxTasker"
        mc:Ignorable="d"
        Title="QueueWindow" Height="800" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0" Margin="10">
            <DataGrid 
                x:Name="nodeQueue" 
                ItemsSource="{Binding activeCollection}" 
                AutoGenerateColumns="False"
                AlternatingRowBackground="LightBlue" 
                AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Type" Binding="{Binding Path=nodeType}" IsReadOnly="true"/>
                    <DataGridTextColumn Header="Label" Binding="{Binding Path=nodeLabel}" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
        <Button Grid.Row="1" Command="{Binding Path=addExampleItems_cmd}">AddExamples</Button>

    </Grid>
</Window>
